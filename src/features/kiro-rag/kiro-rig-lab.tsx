/**
 * Retained as a compatibility component for the previous Kiro rig playground.
 * The public Kiro Rag page no longer exposes raw anatomical sliders because
 * they allowed invalid poses. Runtime testing now happens through the
 * application-state behavior probe on the page.
 */
export default function KiroRigLab() {
  return null;
}
